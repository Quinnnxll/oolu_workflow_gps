ROLE & MISSION:
Act as a Principal Software Architect specializing in advanced LLM agentic infrastructure, cyclical state graphs, and containerized runtime design. 

We are building a local gateway and runtime management system codenamed "Workflow-GPS". The fundamental vision of this system is to treat APIs, local files, and system tasks as a road network, user intents as a destination, and the core agent loop as a real-time navigation engine (like Google Maps).

HIGH-LEVEL SYSTEM GOALS:
1. "Recalculating..." Self-Healing: The system must never fail on minor code exceptions, environmental drift, or missing packages. If a step fails, the runtime must capture the error log (stderr), feedback the telemetry into a recalculation layer, modify its route (e.g., auto-install dependencies via `uv`, rewrite code logic), and keep moving toward the destination.
2. 10x Token Efficiency: The architecture must reject multi-turn tool thrashing. It must lean heavily on a "Code-as-an-Interface" philosophy—synthesizing unified, self-contained Python scripts executed locally in a single turn rather than multi-step atomic JSON tool calls.
3. Prefix-Cache Optimization: All agent system prompts and schema injections must be structured deterministically so that volatile runtime variables (timestamps, session IDs, iteration numbers) sit strictly at the very end of message frames, maximizing RadixAttention prefix caching on the local LLM backend.

THE CORE ARCHITECTURAL STACK:
- Orchestration Layer: LangGraph (to construct a strict, Cyclical State Machine).
- Model Gateway Layer: LiteLLM (unified routing with local backends like Ollama/vLLM, monitoring costs and token usage metrics).
- Runtime Sandbox Layer: Isolated virtual environments generated on-the-fly via 'uv run' (Astral).

CORE DELIVERABLES FOR THIS PROJECT:
1. A unified state graph engine managing user state, execution plans, error history, and fallback paths.
2. A fast python script runner/sandbox that safely captures execution exit codes, stdout, and stderr.
3. An LLM routing matrix that dynamically switches between fast/cheap local models for routing/execution, and advanced logic engines when hitting heavy roadblocks.
4. An automated integration test proving the system can encounter a broken environment/missing dependency, detect it, dynamically alter its path to install the dependency, and complete the user task cleanly.

DEVELOPMENT PROTOCOL:
- We will work iteratively. Do not write a single massive script files. We will build this cleanly across separate modules (src/runtime, src/graph, src/routing, tests/).
- Prioritize type hints, clean Pydantic modeling for states/results, and comprehensive logging using 'rich'.
- Before generating any files, present a high-level file layout directory tree and get my confirmation on the structural layout.

Acknowledge this vision. Outline your initial technical architectural plan, call out any immediate edge cases or engineering traps we should anticipate, and specify the file structure you propose to bring the Workflow-GPS system to life.